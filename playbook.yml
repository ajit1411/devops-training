---
- name: DevOps Training
  hosts: testserver
  become: true
  become_user: root
  tasks:
    - name: Install Git
      package:
        name: git
        state: present
    - name: Install Docker
      package:
        name: docker.io
        state: present
    - name: Check whether we already have project repo
      stat:
        path: /home/ubuntu/devops-training
      register: p
    - name: Clone the application
      command: git clone https://github.com/ajit1411/devops-training.git
      when: p.stat.exists == False
    - name: Pull application updates
      shell: cd devops-training && git pull
      when: p.stat.exists == True
    - name: Build docker image
      shell: sudo docker build -t ajitjadhav1411/my-devops-image .
    - name: Run container
      shell: docker run -itd -p 1411:80 --name my-devops-container ajitjadhav1411/my-devops-image